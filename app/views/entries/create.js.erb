var entries = <%= "#{@entries.to_json}".html_safe %> //print out an array of some sort

console.log(entries);


console.log("Sanity");
// New entries will always be uncleared, and will never change the cleared balance



array_of_trs = $("tr");

$.each(array_of_trs, function(index, value){
  var entryObject;
  console.log("array_index= " + index);
  $.each(<%= @entries %>, function(index, entry){
    console.log("Value id: " + value.attr('id'));
    console.log("Entry id: " + entry.id + "Entry index: " + index);
  //   if(value.attr('id') == entry.id){
  //     entryObject = entry
  //     console.log('matched')
  //   }

  //   $(value).data('rank', entryObject.rank)

  //   $(value).find(".balance-cell").html((entryObject.balance_cents/100).toFixed(2))
  });

});


